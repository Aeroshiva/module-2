import{ClientProfile} from "\client_profile.ts";

export class service{
    view_transaction: string|number;
    account_settings: string;
    manage_device: number;
    upgrade_plan: string|number;
}